const init = {
    cars: ['BMW']
}

export default function reducer(state, action, args){
    switch (action){
        // case 'CREATE':

        //     break
        default: 
            return state
    }
}